# Launch or Focus

this program will launch a program or focus to it if already running

# Example
```bash
$ lof code # launch or focus vscode
$ lof devtools # launch or focus devtools on active chrome tab in current desktop/workspace
```

# Dependencies

- xdotool
